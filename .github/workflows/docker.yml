name: Docker

on:
  push:
    # Only the default branch — pushes to ai/*, edit/*, etc. never build.
    branches: [main]
    # Only application/build logic. Note: docs/** is intentionally NOT listed,
    # so doc edits (incl. those merged into main via the app) do NOT trigger a
    # rebuild — the image carries no docs (GitHub is the source of truth).
    paths:
      - "apps/**"
      - "packages/**"
      - "Dockerfile"
      - "package.json"
      - "package-lock.json"
      - ".github/workflows/docker.yml"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Generate version
        id: version
        run: |
          VERSION="$(date -u +%Y.%-m%d).${{ github.run_number }}"
          echo "VERSION=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "Version: ${VERSION}"

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push
        uses: docker/build-push-action@v5
        with:
          # Single container: the root Dockerfile builds web + api and the
          # backend serves the frontend. Context = monorepo root so it can
          # COPY apps/* and packages/*.
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/specification-hub:latest
            ghcr.io/${{ github.repository_owner }}/specification-hub:${{ steps.version.outputs.VERSION }}

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Trigger Coolify deploys
        env:
          # Each prod is its own Coolify resource (own docs repo + brand), so the
          # same image is deployed to multiple webhooks. WEBHOOK_2 is optional.
          COOLIFY_WEBHOOK: ${{ secrets.COOLIFY_WEBHOOK }}
          COOLIFY_WEBHOOK_2: ${{ secrets.COOLIFY_WEBHOOK_2 }}
          COOLIFY_TOKEN: ${{ secrets.COOLIFY_TOKEN }}
        run: |
          trigger() {
            [ -z "$1" ] && return 0
            echo "Deploying → $1"
            curl -f -X POST "$1" -H "Authorization: Bearer $COOLIFY_TOKEN"
          }
          if [ -z "$COOLIFY_WEBHOOK" ] && [ -z "$COOLIFY_WEBHOOK_2" ]; then
            echo "No COOLIFY_WEBHOOK* secrets set — skipping deploy."
            exit 0
          fi
          trigger "$COOLIFY_WEBHOOK"
          trigger "$COOLIFY_WEBHOOK_2"
